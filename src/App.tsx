import { Routes } from "react-router-dom"; 
import routes from "./pages/routes/routes"; 

const App = () => {
   return (
      <Routes>
         {routes}
      </Routes>
   );
};

export default App;
